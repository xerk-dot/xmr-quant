# =============================================================================
# MONERO TRADING BOT - ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and fill in your actual values
# Command: cp env.example .env
# DO NOT commit .env to git (it's in .gitignore)
# =============================================================================

# =============================================================================
# ENVIRONMENT
# =============================================================================
ENVIRONMENT=development
LOG_LEVEL=INFO

# =============================================================================
# EXCHANGE API CREDENTIALS
# =============================================================================
# Binance - https://www.binance.com/en/my/settings/api-management
BINANCE_API_KEY=your_binance_api_key_here
BINANCE_SECRET=your_binance_secret_here

# Kraken - https://www.kraken.com/u/security/api
KRAKEN_API_KEY=your_kraken_api_key_here
KRAKEN_SECRET=your_kraken_secret_here

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL - stores trade history, positions, signals
DB_HOST=localhost
DB_PORT=5432
DB_NAME=monero_trading
DB_USER=trading_bot
DB_PASSWORD=your_secure_password_here

# Redis - caching and task queue
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# InfluxDB - time-series price data
INFLUXDB_URL=http://localhost:8086
INFLUXDB_TOKEN=your_influxdb_token_here
INFLUXDB_ORG=trading_bot
INFLUXDB_BUCKET=market_data

# =============================================================================
# TELEGRAM ALERTS (REQUIRED for notifications)
# =============================================================================
# Get bot token from @BotFather on Telegram
# Get chat ID by messaging your bot and visiting:
# https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_telegram_chat_id_here

# =============================================================================
# NEWS MONITORING (OPTIONAL - Set to false if you don't have API keys)
# =============================================================================
NEWS_MONITORING_ENABLED=false

# Twitter API v2 - https://developer.twitter.com/en/portal/dashboard
# Requires Essential or higher tier ($100+/month for adequate rate limits)
TWITTER_BEARER_TOKEN=

# LLM Provider - Choose 'openai' or 'anthropic'
NEWS_LLM_PROVIDER=openai

# OpenAI - https://platform.openai.com/api-keys
# Costs: ~$0.30 per 1000 classifications with gpt-4o-mini
OPENAI_API_KEY=

# Anthropic - https://console.anthropic.com/
# Costs: ~$0.50 per 1000 classifications with claude-3-haiku
ANTHROPIC_API_KEY=

# Optional: Override default model
# NEWS_LLM_MODEL=gpt-4o-mini  # or claude-3-haiku-20240307

# News monitoring settings
NEWS_CHECK_INTERVAL_MINUTES=30
NEWS_AGGREGATION_WINDOW_HOURS=2
NEWS_STRATEGY_WEIGHT=0.10

# =============================================================================
# DARKNET MONITORING (OPTIONAL - EXPERIMENTAL)
# =============================================================================
# WARNING: This feature is experimental and requires:
# 1. Tor running locally (brew install tor / apt install tor)
# 2. Valid .onion addresses (see docs/DARKNET_QUICK_START.md)
# 3. Manual configuration of marketplace addresses
# 
# Set to false unless you know what you're doing
DARKNET_MONITORING_ENABLED=false

# Tor proxy settings (default for system tor)
DARKNET_TOR_PROXY_HOST=127.0.0.1
DARKNET_TOR_PROXY_PORT=9050

# Darknet strategy parameters
DARKNET_UPDATE_INTERVAL_HOURS=24
DARKNET_STRATEGY_WEIGHT=0.05
DARKNET_BULLISH_THRESHOLD=60.0
DARKNET_BEARISH_THRESHOLD=35.0
DARKNET_MIN_CONFIDENCE=0.5

# =============================================================================
# RISK MANAGEMENT PARAMETERS
# =============================================================================
# Position sizing
MAX_POSITION_SIZE=0.02              # 2% of portfolio per trade
MAX_PORTFOLIO_EXPOSURE=0.3          # 30% total exposure
MIN_RISK_REWARD_RATIO=1.5           # Minimum 1.5:1 R:R

# Stop loss
DEFAULT_STOP_LOSS_ATR_MULTIPLIER=2.5

# Daily limits
MAX_DAILY_LOSS=0.05                 # 5% daily loss limit
MAX_CONSECUTIVE_LOSSES=5            # Stop after 5 losses in a row

# =============================================================================
# STRATEGY WEIGHTS (Must sum to 1.0 when all enabled)
# =============================================================================
# Default weights when all strategies enabled:
# - BTCCorrelation: 0.40 (primary edge)
# - NewsSentiment: 0.10 (if enabled)
# - DarknetAdoption: 0.05 (if enabled)
# - TrendFollowing: 0.125
# - MeanReversion: 0.125
# - XGBoostML: 0.25
#
# Weights auto-adjust if strategies are disabled

# =============================================================================
# NOTES
# =============================================================================
# MINIMUM VIABLE SETUP (to just get it running):
# 1. Fill in one exchange API (Binance or Kraken)
# 2. Leave databases as localhost (if using docker-compose)
# 3. Get Telegram bot token and chat ID
# 4. Set NEWS_MONITORING_ENABLED=false
# 5. Set DARKNET_MONITORING_ENABLED=false
# 6. Run: docker-compose up -d
#
# FULL SETUP (for all features):
# See SETUP.md for detailed instructions on getting all API keys

